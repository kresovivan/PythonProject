"""
Научись думать как программист
Аллен Б. Дауни"""

"""
Несколько основных инструкций универсальны для любого языка:
— ввод данных (input):
Получение данных с клавиатуры, из файла, по сети или с другого 
устройства.
— вывод данных (output):
Отображение данных на экране, сохранение их в файл, отправка 
по сети и так далее.
— математические операции (math):
Выполнение основных математических операций, таких как сложение 
и умножение.
— условное выполнение (conditional execution):
Проверка определенных условий и выполнение соответствующего 
кода.
— повторение (repetition):
Выполнение некоторого действия несколько раз, часто с некоторыми 
изменениями.

Таким образом, вы можете представить программирование как 
процесс разбиения большой и сложной задачи на всё более мелкие подзадачи, 
пока подзадачи не станут достаточно простыми, чтобы их можно было 
сформулировать с помощью одной из этих инструкций


Формальные языки разрабатываются людьми для определенных целей. 
Например, математические символы представляют собой формальный 
язык, который особенно хорош для обозначения отношений между числами 
и символами. Химики используют формальный язык для представления 
химической структуры молекул. И самое важное:
Языки программирования — это формальные языки, предназначенные для выражения вычислений.
Формальные языки, как правило, имеют строгие синтаксические правила, 
которым подчиняется структура кода. Например, в математике утверждение 
3 + 3 = 6 имеет правильный синтаксис, а 3 + = 3$6 — нет. В химии H2
O — 
синтаксически правильная формула, а 2Zz — нет.


Фундамент: «Алфавит и грамматика»
Это то, как компьютер понимает ваши команды. Без этого код не запустится.

Синтаксис (Syntax): Правила написания команд. Как в русском языке есть правило «жи-ши пиши с буквой И», 
так и в Python есть правило ставить двоеточие после if.

Семантика (Semantics): Смысл команд. Что значит +? 
Сложить числа или склеить строки?

Типы данных (Data Types): Что за «кирпичики» есть в языке? Числа (int, float), 
строки (str), булевы значения (bool), списки (list), словари (dict)."""


"""СЛОВАРЬ ТЕРМИНОВ:

Решение задачи (problem solving):
Процесс формулирования задачи, поиска решения и его реализации.

Высокоуровневый язык (high-level language):
Язык программирования, разработанный, чтобы программистам было 
легко и удобно его использовать. Python — это высокоуровневый язык.

Низкоуровневый язык (low-level language):
Язык программирования, приближенный к инструкциям, которые понимает компьютер, но сложный для человека; также называется «машинным 
языком», или «языком ассемблера».

Портативность (portability):
Способность программы функционировать на компьютерах разного типа.

Интерпретатор (interpreter):
Программа, которая анализирует, обрабатывает, считывает и выполняет 
исходный код. 

Приглашение (prompt):
Символы, отображаемые интерпретатором для обозначения ожидания 
ввода пользователем.

Программа (program):
Набор инструкций, определяющих вычисления.

Инструкция печати (print statement):
Инструкция, которая заставляет интерпретатор Python отображать 
значение на экране.

Оператор (operator):
Специальный символ (или символы), позволяющий выполнить простые 
вычисления, такие как сложение, умножение или соединение строк.

Значение (value):
Одна из основных единиц данных, таких как число или строка, которыми 
манипулирует программа.

Тип (type):
Тип — это категория значения. Основные типы переменных: целые числа 
(int), числа с плавающей точкой (float), строки (str).

Целое число (int):
Тип, который представляет целые числа.

Число с плавающей точкой (floating-point):
Тип, представляющий числа с дробной частью.

Строка (str):
Тип, который представляет собой последовательности символов.

Естественный язык (natural language):
Язык, на котором говорят люди и который развивался естественным 
путем.

Формальный язык (formal language):
Язык, который люди разработали для определенных целей, таких как 
представление математических концепций или компьютерных программ; 
все языки программирования — формальные языки.

Токен (token):
Один из основных элементов синтаксической структуры программы, 
аналог «слова» в естественном языке.

Синтаксис (syntax):
Правила, определяющие структуру исходного кода программы.

Парсинг (parsing):
Разбор кода программы и анализ синтаксической структуры.

Ошибка (bug):
Ошибка в программе

Переменная (variable):
Имя, которое ссылается на значение.

Присваивание (assignment):
Назначение значения переменной.

Диаграмма состояний (state diagram):
Графическое представление набора переменных и значений, к которым 
они относятся.

Зарезервированное или ключевое слово (keyword):
Слово, которое используется в процессе анализа (парсинга) исходного 
кода программы интерпретатором. Такие слова, как if, def и while, нельзя 
использовать при определении имен переменных.

Операнд (operand):
Одно из значений, которыми оперирует оператор *.

Выражение (expression):
Комбинация значений, переменных и операторов, которая возвращает 
результат.

Вычисление (evaluate):
Упрощение выражения и преобразование операндов для получения 
конечного результата.

Инструкция (statement):
Часть кода, представляющая команду или действие. Вы уже знакомы 
с инструкциями присвоения и печати.

Выполнение (execute):
Выполнение инструкции интерпретатором.

Интерактивный режим (interactive mode):
Способ использования интерпретатора Python путем ввода кода в командной строке.

Выполнение скрипта (script mode):
Способ использования интерпретатора Python для чтения исходного 
кода из файла и его выполнения.

Скрипт (script):
Исходный код программы, записанный в файл.

Приоритет операций (order of operations):
Правила, регулирующие порядок, в котором вычисляются выражения, 
включающие несколько операторов и операндов.

Конкатенация (concatenate):
Объединение двух операндов.

Комментарий (comment):
Информация, поясняющая исходный код. Не влияет на ход выполнения 
программы.

Синтаксическая ошибка (syntax error):
Ошибка в программе, которая делает невозможным синтаксический 
анализ и, следовательно, не позволяет интерпретировать программу.

Исключение (exception):
Ошибка, обнаруженная во время выполнения программы.

Семантика (semantics):
Смысловое значение, предназначение программы.

Семантическая ошибка (semantic error):
Ошибка, которая заставляет программу работать не так, как предполагал 
разработчик.


"""


"""ВЫЗОВ ФУНКЦИИ
 type(42)
<class 'int'>

Имя функции — type. Выражение в скобках называется аргументом 
(argument) функции. Если необходимо передать несколько аргументов — они 
указываются через запятую. Результат выполнения функции type() — вывод 
типа аргумента.
Обычно говорят, что функция «принимает» аргументы и «возвращает» 
значение или результат. 
Python поддерживает функции, которые преобразуют значения из одного 
типа в другой. Функция int() принимает любое значение и, если может, 
преобразует его в целое число или «жалуется» на те, которые не может 
преобразовать:
"""

print(type(42))  # <class 'int'>
print(int("32"))  # 32
# print(int('Привет')) #ValueError: invalid literal for int(): 'Привет'

"""Функция int() может преобразовывать значения с плавающей точкой 
в целые числа, но не округляет, а просто отбрасывает дробную часть:"""
print(int(3.99999))
print(int(-2.3))


"""Функция float() преобразует целые числа и строки в числа с плавающей 
точкой:"""
print(float(32))
# 32.0
print(float("3.14159"))
# 3.14159

"""Наконец, str преобразует принимаемый аргумент в строку:"""
print(str(32))
print(str(3.14159))


"""МАТЕМАТИЧЕСКИЕ ФУНКЦИИ
В Python входит математический модуль, который предоставляет доступ 
к большинству известных математических функций. Модуль (module) — это 
файл, содержащий подборку связанных функций.
Прежде чем мы сможем использовать функции в модуле, мы должны 
импортировать его с помощью инструкции импорта (import statement):"""
import math

from numpy import angle

"""Эта инструкция создает объект модуля (module object) math. Если вы 
вызовете объект модуля в интерактивном режиме, вы получите некоторую 
информацию о нем:"""
print(math)  # <module 'math' (built-in)>

"""Объект модуля содержит функции, переменные и константы, определенные в модуле. 
Чтобы получить доступ к одной из функций, вы должны 
указать имя модуля и имя функции, разделенные точкой. Такой формат 
называется точечной нотацией (dot notation)."""
noise_power = 0.0001
signal_power = 0.01
ratio = signal_power / noise_power
decibels = 10 * math.log10(ratio)
radians = 0.7
height = math.sin(radians)
print(height)
print(decibels)

"""В первом примере используется функция math.log10() для вычисления отношения сигнал/шум 
в децибелах (при условии, что определены переменные 
signal_power и noise_power). Математический модуль также предоставляет 
функцию log(), которая вычисляет логарифм по основанию e.

Второй пример находит синус для значения переменной radians. Имя 
переменной служит подсказкой, что sin() и другие тригонометрические 
функции (cos(), tan() и так далее) принимают аргументы в радианах. Чтобы 
преобразовать градусы в радианы, разделите значение на 180 и умножьте на π:"""

degrees = 45
radians = degrees / 180.0 * math.pi
height = math.sin(radians)
print(height)  # 0.7071067811865475

"""Выражение math.pi возвращает константу pi из математического модуля. 
Ее значение является приближенным к числу π с точностью до 15 знаков.
Если вы знаете тригонометрию, вы можете проверить предыдущий 
результат, сравнив его с квадратным корнем из 2, деленным на 2:"""

print(math.sqrt(2) / 2)


"""КОМПОЗИЦИИ
До сих пор мы рассматривали элементы программы — переменные, выражения и инструкции — изолированно, 
не говоря о том, как их объединять.
Одна из наиболее полезных возможностей языков программирования 
заключается в их способности брать небольшие строительные блоки и составлять из них композицию.
Например, аргумент функции может быть выражением любого типа, включая арифметические операции:

def greet(name):      # name — это аргумент
    return f"Привет, {name}!"

print(greet("Анна"))  # "Анна" — это значение аргумента


def square(x):
    return x * x

# Передаём простое значение
print(square(5))        # 25
# Передаём арифметическое выражение
print(square(2 + 3))    # ТОЖЕ 25!

Python делает три шага:
Вычисляет выражение: 2 + 3 → 5
Передаёт результат в функцию: square(5)
Выполняет функцию: 5 * 5 → 25

"""
x = math.sin(degrees / 360.0 * 2 * math.pi)
print(x)  # 0.7071067811865475
"""И даже вызовом других функций:"""
x = math.exp(math.log(x + 1))
print(x)  # 0.7071067811865475

"""Практически везде, где вы можете поместить значение, можно поместить 
произвольное выражение, с одним исключением: левая часть инструкции
присваивания должна быть именем переменной. Любое другое выражение 
слева будет считаться синтаксической ошибкой (мы увидим исключения 
из этого правила позже)."""
hours = 2
minutes = hours * 60  # правильно
print(minutes)  # 120
# hours * 60 = minutes # неправильно! SyntaxError: can't assign to operator


"""ДОБАВЛЕНИЕ НОВЫХ ФУНКЦИЙ
До сих пор мы использовали только функции, которые изначально присутствуют в языке Python, 
но можно создавать и новые функции. Для определения новой функции (function definition) 
необходимо указать ее имя 
и последовательность инструкций, которые выполняются при ее вызове"""


def print_lyrics():
    print("Я дровосек, и со мной все в порядке.")
    print("Я работал весь день, и теперь я в достатке.")


"""Слово def — ключевое; оно указывает, что это определение функции. 
Имя функции — print_lyrics. 
Правила для имен функций те же, что и для 
имен переменных: латинские буквы, цифры и подчеркивание допустимы, 
но первый символ не может быть цифрой. 
Вы не можете использовать зарезервированные (ключевые) слова в качестве имен функций, 
и вам следует избегать использования переменных и функций с одинаковыми именами
Пустые скобки после имени указывают, что эта функция не принимает 
никаких аргументов.

Первая строка в функции называется заголовком (header), весь остальной 
ее код называется телом (body). Заголовок должен заканчиваться двоеточием, 
а тело должно иметь отступ. По правилам, отступ всегда равен четырем 
пробелам. Тело может содержать любое количество инструкций.

Строки в инструкциях печати заключены в кавычки. Можно использовать 
как одинарные, так и двойные кавычки, главное — в паре; большинство 
программистов используют одинарные кавычки, за исключением случаев, 
когда в строке используется апостроф (по сути, та же одинарная кавычка).

Все кавычки (одинарные и двойные) должны быть «прямыми», их набирают клавишей, 
расположенной рядом с Enter на клавиатуре. «Косые» кавычки 
и кавычки-елочки (“”, «») нельзя использовать в коде на языке Python.

Определение функции создает объект функции (function object), который имеет тип function
"""

print(print_lyrics)  # <function print_lyrics at 0x000002D4749A3320>
print(type(print_lyrics))  # <class 'function'>

"""Синтаксис для вызова новой функции такой же, как и для встроенных 
функций:"""
print_lyrics()  # Вызов функции

"""Как только вы определили функцию, вы можете использовать ее внутри 
другой функции. Например, чтобы повторить предыдущий припев, мы 
могли бы написать функцию с именем repeat_lyrics():"""


def repeat_lyrics():
    print_lyrics()
    print_lyrics()


repeat_lyrics()

"""ОПРЕДЕЛЕНИЕ И ИСПОЛЬЗОВАНИЕ
Собрав воедино фрагменты кода из предыдущего раздела, мы получим 
такую программу:"""


def print_lyrics():
    print("Я дровосек, и со мной все в порядке.")
    print("Я работал весь день, и теперь я в достатке.")


def repeat_lyrics():
    print_lyrics()
    print_lyrics()


repeat_lyrics()

"""Эта программа определяет две функции: print_lyrics() и repeat_lyrics(). 
Определения функций выполняются интерпретатором так же, как и другие инструкции, 
но в результате создаются объекты функции, а инструкции внутри функций 
не выполняются до тех пор, пока эти функции не будут вызваны.

Очевидно, вы должны создать функцию, прежде чем сможете ее выполнить. 
Другими словами, определение функции должно быть выполнено до вызова функции

В качестве упражнения переместите последнюю строку этой программы 
вверх, чтобы перед определениями появился вызов функции. Запустите 
программу и посмотрите, какое сообщение об ошибке появится
"""
# repeat_lyrics1() #NameError: name 'repeat_lyrics1' is not defined. Did you mean: 'repeat_lyrics'?


def print_lyrics1():
    print("Я дровосек, и со мной все в порядке.")
    print("Я работал весь день, и теперь я в достатке.")


def repeat_lyrics1():
    print_lyrics1()
    print_lyrics1()


"""Теперь переместите вызов функции назад, а определение функции 
print_lyrics() поставьте после определения repeat_lyrics(). Что происходит 
при запуске этой программы?"""


def repeat_lyrics2():
    print_lyrics2()


def print_lyrics2():
    print("Я дровосек № 2, и со мной все в порядке.")
    print("Я работал все 2 дня, и теперь я в достатке.")


repeat_lyrics2()  # Я дровосек № 2, и со мной все в порядке. Я работал все 2 дня, и теперь я в достатке.

"""ПОРЯДОК ВЫПОЛНЕНИЯ!!!
Чтобы убедиться, что функция определена перед ее первым использованием, 
нужно знать порядок выполнения инструкций (flow of execution).
1.Выполнение программы всегда начинается с первой инструкции. 
2.Далее инструкции выполняются по одной, сверху вниз.
3.Определения функций в коде программы только создают объект функции!!!
4.Инструкции, находящиеся в теле функции, не выполняются, пока она не будет вызвана

Вызов функции похож на небольшую остановку. 
Вместо перехода к следующей инструкции интерпретатор переходит к телу функции, 
выполняет инструкции там, а затем возвращается, чтобы продолжить с того места, где 
остановился.

Это звучит просто, пока вы не узнаете, что одна функция может вызывать другую. 
В середине одной функции программе может потребоваться 
выполнить инструкции другой функции. Затем при запуске этой новой 
функции программе может потребоваться запустить еще одну функцию!

К счастью, Python хорошо отслеживает, где находится интерпретатор, 
поэтому каждый раз, когда функция завершает работу, интерпретатор 
возвращается туда, где она остановилась в вызывающей ее функции. 
Когда он доходит до конца программы, она завершает работу

Таким образом, при изучении кода программы необходимо следить 
за порядком выполнения инструкций, так как он не всегда происходит 
сверху вниз.
"""


"""
ПАРАМЕТРЫ И АРГУМЕНТЫ
Некоторые из функций, которые мы видели, требуют передачи аргументов. 
Например, когда вы вызываете функцию math.sin(), вы передаете число в качестве аргумента. 
Некоторые функции принимают более одного аргумента: 
функция math.pow() принимает два: основание и экспоненту.

Внутри функции аргументы присваиваются переменным, называемым параметрами (parameters)!!!
Ниже представлено определение функции, которая принимает аргумент:

"""


def print_twice(bruce):
    print(bruce)
    print(bruce)


print_twice("Спам")

print_twice(math.pi)

"""
Правила композиции для встроенных функций применяют и к новым, 
определяемым программистом, функциям. 
Поэтому мы можем использовать 
любые выражения в качестве аргумента для функции print_twice():

"""

print_twice("Спам " * 4)
print_twice(math.cos(math.pi))

"""
Аргумент вычисляется до вызова функции, поэтому в примерах выражения 'Спам' * 4 и math.cos(math.pi) 
вычисляются только один раз. 
Вы также можете использовать переменную в качестве аргумента:
"""

michael = "Эрик, полпчелы."
print_twice(michael)

"""
Имя переменной, которую мы передаем в качестве аргумента (michael), 
не имеет ничего общего с именем параметра (bruce). 
!!! Неважно, какое имя переменной было у значения в вызывающей функции; 
"""

"""
ПЕРЕМЕННЫЕ И ПАРАМЕТРЫ ВНУТРИ ФУНКЦИЙ — 
ЛОКАЛЬНЫ
Когда вы создаете переменную внутри функции, она является локальной 
(local), что означает, что она существует только внутри функции. Например:
"""


def cat_twice(part1, part2):
    cat = part1 + part2
    print_twice(cat)


"""Эта функция принимает два аргумента, объединяет их и выводит результат дважды. 
Ниже представлен пример, где она используется:"""

line1 = "Привет, "
line2 = "Иван!"
cat_twice(line1, line2)

"""
Параметры также локальны. Например, за пределами функции print_twice() нет ничего, 
что относится к bruce.
Когда выполнение функции cat_twice() завершается, переменная cat
уничтожается. Если мы попытаемся вывести ее, то получим исключение:
"""

# print(cat) #name 'cat' is not defined

"""
СТЕКОВЫЕ ДИАГРАММЫ
Чтобы отследить, какие переменные и где можно использовать, полезно 
нарисовать стековую диаграмму. Как и диаграммы состояний, стековые 
диаграммы отображают значение каждой переменной, но они также показывают функцию, 
которой принадлежит каждая переменная.

Каждая функция представлена отдельным фреймом. 
Фрейм — это блок с именем функции рядом с ним, а также параметрами и переменными функции 
внутри него. 
┌─────────────────────────────────────────────────────────────────────────────┐
│                          ПОРЯДОК ВЫПОЛНЕНИЯ                                 │
├─────────────────────────────────────────────────────────────────────────────┤
│  1. __main__ вызывает cat_twice                                             │
│  2. cat_twice вызывает cat                                                  │
│  3. cat вызывает print_twice                                                │
│  4. print_twice выполняется (печатает)                                      │
│  5. Возврат в cat                                                           │
│  6. Возврат в cat_twice                                                     │
│  7. Возврат в __main__                                                      │
└─────────────────────────────────────────────────────────────────────────────┘
Фреймы расположены в стеке, который указывает, какая функция вызвала 
какую, и так далее. 
В этом примере функция print_twice() была вызвана функцией cat_twice(), 
а функция cat_twice() вызвана __main__ — это имя  самого верхнего фрейма. 
Когда вы создаете переменную вне какой-либо функции, она принадлежит __main__.

Каждый параметр ссылается на то же значение, что и соответствующий 
ему аргумент. 
Так, part1 имеет то же значение, что и line1; 
part2 имеет то же значение, что и line2; 
а bruce имеет то же значение, что и cat.

Если во время вызова функции возникает ошибка, Python выводит имя 
функции, имя функции, вызвавшей ее, и имя функции, вызвавшей ту, вплоть 
до __main__.


Например, если вы попытаетесь получить доступ к cat из print_twice, 
вы получите следующую ошибку NameError:

Traceback (innermost last):
File "test.py", line 13, in __main__
cat_twice(line1, line2)
File "test.py", line 5, in cat_twice
print_twice(cat)
File "test.py", line 9, in print_twice
print(cat)
NameError: name 'cat' is not defined

Этот список функций называется трассировкой (traceback). Он сообщает 
вам, в каком программном файле произошла ошибка и какая строка какой 
функции выполнялась в это время. Он также показывает строку кода, 
которая вызвала ошибку.
Порядок функций в трассировке совпадает с порядком фреймов в стековой диаграмме. 
Функция, в которой произошла ошибка, находится внизу!!!
"""


"""
РЕЗУЛЬТАТИВНЫЕ ФУНКЦИИ 
И VOID-ФУНКЦИИ

Некоторые из функций, которые мы использовали, например математические, возвращают результат. 
За неимением другого решения я называю их результативные (fruitful) функции. 
Другие функции, вроде print_twice(), выполняют действие, но не возвращают значение. 
Они называются void-функциями (или иногда — функциями, не возвращающими результат).

Чаще всего при вызове функции, возвращающей значение, вы планируете что-то сделать с результатом; 
например, вы можете присвоить его переменной или использовать в составе выражения:
"""

radians1 = degrees / 180.0 * math.pi
x = math.cos(radians1)
golden = (math.sqrt(5) + 1) / 2
print(golden)

"""
Void-функции могут выводить что-то на экран или иметь какой-то другой 
эффект, но они не возвращают никакого значения. 
Если вы попробуете присвоить такую функцию переменной, 
вы получите специальное значение None:
"""
result = print_twice("Бинго")
print(result)

"""Значение None — это не то же самое, что строка 'None'. Это специальное 
значение, которое имеет собственный тип:"""

print(type(None))  # <class 'NoneType'>

"""Все функции, которые мы написали до сих пор, не возвращали никакой 
результат. В следующих главах мы начнем писать функции, которые возвращают значения.
"""


"""
ЗАЧЕМ НУЖНЫ ФУНКЦИИ?
Поясним, почему стоит тратить силы и делить код программы на функции!!!

— Создание новой функции позволяет присвоить имя группе инструкций, что упрощает чтение 
и отладку программы.

— Функции уменьшают объем кода программы за счет исключения 
повторяющегося кода. 
А если вы захотите что-то изменить, нужно будет сделать это только в одном месте.

— Разделение кода большой программы на функции упрощает отладку, так как можно 
тестировать каждую из функций индивидуально.

— Хорошо продуманные функции зачастую полезны во многих программах. 
Как только вы напишете и отладите одну, вы сможете использовать ее снова и снова.
"""


"""
ОТЛАДКА
Один из важнейших навыков разработчика — отладка. Иногда она раздражает, 
но все равно остается едва ли не самой интеллектуально насыщенной, 
сложной и интересной частью программирования.

Отладка в чем-то похожа на детективную работу. Вы сталкиваетесь  с подсказками и 
должны воссоздать процессы и события, которые привели к полученным результатам.

Отладка похожа на экспериментальную науку. Как только вы поймете, что 
что-то идет не так, вы измените свою программу и попробуете снова. 

Если ваша гипотеза оказалась верной, вы можете предсказать результат модификации 
и на шаг приблизиться к работающей программе. 

Если ваша гипотеза была неверна, вы должны придумать новую. 
Как заметил Шерлок Холмс: 
"Если вы отбросите все невозможное, то всё, что останется, 
и будет правдой, какой бы невероятной она ни была."
(Артур Конан Дойл, «Знак Четырех»).

Для некоторых людей программирование и отладка — одно и то же. 
То есть программирование — это процесс постепенной отладки программы, 
пока она не выполнит то, что вы хотите. Идея в том, что вы должны начать 
с работающей программы и вносить небольшие изменения, отлаживая их 
по мере продвижения.

Например, Linux — это операционная система, содержащая миллионы 
строк кода. Но началась она как простая программа, которую Линус Торвальдс 
использовал для исследования чипа Intel 80386. По словам Ларри 
Гринфилда, «одним из ранних проектов Линуса была программа, которая 
переключалась между печатью строк “AAAA” и “BBBB”.
"""

"""
СЛОВАРЬ ТЕРМИНОВ
Функция: 
Именованная последовательность инструкций, выполняющая некоторые 
полезные вычисления. Функции не обязательно принимают аргументы 
и возвращают значения.

Определение функции:
Инструкция, которая создает новую функцию с указанием ее имени, 
параметров и инструкций, которые она выполняет.

Объект функции:
Значение, созданное определением функции. 
Имя функции — это переменная, которая ссылается на объект функции.

Заголовок:
Первая строка определения функции.

Тело:
Последовательность инструкций внутри определения функции.

Параметр:
Имя, используемое внутри функции для ссылки на значение, переданное 
в качестве аргумента.

Вызов функции:
Инструкция, которая запускает функцию. Она состоит из имени функции, за которым следует 
список аргументов в скобках.

Аргумент:
Значение, предоставляемое функции при ее вызове. 
Это значение присваивается соответствующему параметру в функции.

Локальная переменная:
Переменная, определенная внутри функции. Локальная переменная 
может использоваться только внутри ее функции.

Возвращаемое значение:
Результат функции. Если вызов функции используется в качестве выражения, 
возвращаемое значение является значением выражения.

Функция, возвращающая значение/результат:
Функция, которая возвращает значение/результат.

Void-функция (функция, не возвращающая результат):
Функция, которая всегда возвращает None.

None:
Специальное значение, возвращаемое функциями, которые не возвращают результат.


Модуль:
Файл, содержащий набор связанных функций и других определений.

Инструкция импорта:
Инструкция, которая считывает файл модуля и создает объект модуля.

Объект модуля:
Значение, созданное инструкцией import, через которое можно получить 
доступ к модулю и его функциям.

Точечная нотация:
Синтаксис для вызова функции модуля, для чего нужно указать имя 
модуля, а за ним — точку и имя функции.

Композиция:
Использование выражения в составе большего выражения или 
инструкции в части другой, более крупной инструкции.

Порядок выполнения:
Порядок, в котором выполняются инструкции.

Стековая диаграмма:
Графическое представление стека функций, их переменных и значений, 
на которые они ссылаются.

Фрейм:
Блок в диаграмме стека, представляющий вызов функции. Он содержит 
локальные переменные и параметры функции.

Трассировка:
Список функций, которые выполняются, распечатывается при возникновении ошибки (исключения).
"""


"""
УПРАЖНЕНИЯ
Упражнение 3.1
Напишите функцию right_justify(), которая принимает строку s 
в качестве параметра и печатает строку с достаточным количеством пробелов 
в начале строки, так чтобы последняя буква строки находилась в столбце 70 
на экране:
Совет. Используйте конкатенацию и повторение строк. Кроме того, 
Python содержит встроенную функцию len(), которая возвращает длину 
строки, так что значение len('monty') равно 5
"""


def right_justify(s):
    # Вычисляем, сколько пробелов нужно добавить
    spaces_needed = 70 - len(s)
    # Создаем строку из нужного количества пробелов и добавляем исходную строку
    justified_string = " " * spaces_needed + s
    # Печатаем результат
    print(justified_string)


right_justify("monty")


"""
Упражнение 3.2
Объект функции — это значение, которое вы можете присвоить переменной 
или передать в качестве аргумента. 
Например, do_twice() — это функция, которая принимает объект функции 
в качестве аргумента и вызывает его дважды:
def do_twice (f):
f()
f()
Ниже представлен пример, в котором функция do_twice() используется 
для вызова функции print_spam() дважды:
def print_spam():
print('спам')
do_twice(print_spam)

"""


def do_twice(func, arg):
    """func — параметр, который ожидает функцию (объект функции)
    arg — параметр, который будет передан в функцию func
    func(arg) — вызов переданной функции с аргументом arg

    def print_message(msg):
    print(f"Сообщение: {msg}")

    do_twice(print_message, "Привет!")
    """
    func(arg)
    func(arg)


def print_twice(arg):
    """Prints the argument twice.
    arg: anything printable
    """
    print(arg)


do_twice(print_twice, "spam")
do_twice(print, "goose")
do_twice(print, 42)  # число
do_twice(print, [1, 2, 3])  # список
do_twice(print, {"a": 1})  # словарь
do_twice(print_twice, [5, 10, 20])


""""
# 1. Установите code2flow (1 минута)
python -m pip install code2flow
# 2. Запустите (2 секунды)
code2flow testppp.py -o graph.png

# 3. Откройте graph.png
start graph.png"""

"""
1. Общий механизм (Функции высшего порядка)
В основе лежат функции, которые принимают другие функции как аргументы и вызывают их 
в определенном порядке. Это позволяет создавать сложные структуры из простых блоков.

У нас есть три главные "управляющие" функции:
- do_twice(f): Просто вызывает функцию f два раза.
- do_four(f): Использует do_twice, чтобы вызвать f четыре раза.
- one_four_one(f, g, h): Вызывает f один раз, потом g четыре раза, потом h один раз.

2. Самые простые "кирпичики" (Примитивы)
Это функции, которые печатают один символ и не переводят строку (благодаря end=' '):
print_plus() -> Печатает +.
print_dash() -> Печатает -.
print_bar() -> Печатает |.
print_space() -> Печатает пробел.
print_end() -> Переводит строку (печатает строку перевода).
nothing() -> Функция-пустышка, ничего не делает.

print1beam():
Вызывается one_four_one(nothing, print_dash, print_plus).
Это значит: nothing (ничего), потом 4 раза print_dash (- - - -), потом print_plus (+).
Результат: + - - - - (на самом деле это часть верхней границы ячейки).

print1post():
Вызывается one_four_one(nothing, print_space, print_bar).
Это значит: nothing, потом 4 пробела, потом |.
Результат: | (стенка ячейки и пустота внутри).

4. Сборка линий сетки
Эти функции рисуют целые строки.

print4beams():
Вызывается one_four_one(print_plus, print1beam, print_end).
1. Сначала вызывается print_plus: печатает первый +.
2. Затем 4 раза вызывается print1beam. Каждый вызов print1beam печатает - - - - +.
3. В конце print_end() переводит строку.
Результат: + - - - - + - - - - + - - - - + - - - - + (горизонтальная линия сетки с плюсами на стыках).

print4posts():
Вызывается one_four_one(print_bar, print1post, print_end).
1. Сначала print_bar: печатает первую вертикальную черту |.
2. Затем 4 раза print1post. Каждый print1post печатает пробелы и |.
3. В конце print_end().
Результат: | | | | | (строка, заполненная вертикальными стенками и пробелами).

5. Сборка строки ячеек
print_row():
Вызывается one_four_one(nothing, print4posts, print4beams).
1. Сначала nothing.
2. Затем 4 раза print4posts. Это создает 4 строки с вертикальными стенками (тело ячеек).
3. В конце print4beams рисует горизонтальную линию с плюсами.

print_grid():
Вызывается one_four_one(print4beams, print_row, nothing).
1. Сначала print4beams: рисует верхнюю границу.
2. Затем 4 раза print_row. Каждый print_row рисует:
4 строки вертикальных стенок (print4posts x4).
1 горизонтальную линию (print4beams).
3. В конце nothing.

"""


# ===================================================================
# БАЗОВЫЕ УПРАВЛЯЮЩИЕ ФУНКЦИИ (КОНТРОЛЛЕРЫ)
# ===================================================================
def do_twice(f):
    """
    Принимает функцию-объект f и вызывает её два раза подряд.
    Функции в Python - это объекты, которые можно передавать как аргументы.
    """
    f()  # Вызов функции f (первый раз)
    f()  # Вызов функции f (второй раз)


def do_four(f):
    """
    Принимает функцию f и вызывает её четыре раза,
    используя do_twice дважды (композиция функций).
    """
    do_twice(f)  # Вызывает f два раза
    do_twice(f)  # Ещё два раза = всего 4 вызова


def one_four_one(f, g, h):
    """
    Шаблон "ОДИН-ЧЕТЫРЕ-ОДИН".
    Принимает ТРИ функции как объекты и вызывает их в порядке:
    f() один раз → g() четыре раза → h() один раз.
    """
    f()  # Первая функция вызывается один раз
    do_four(g)  # Вторая функция вызывается четыре раза (через do_four)
    h()  # Третья функция вызывается один раз


# ===================================================================
# ПРИМИТИВНЫЕ ФУНКЦИИ-КИРПИЧИКИ (БАЗОВЫЕ ДЕЙСТВИЯ)
# ===================================================================
def print_plus():
    """Печатает символ '+' без перевода строки."""
    print("+", end=" ")


def print_dash():
    """Печатает символ '-' без перевода строки."""
    print("-", end=" ")


def print_bar():
    """Печатает символ '|' (вертикальная черта) без перевода строки."""
    print("|", end=" ")


def print_space():
    """Печатает пробел без перевода строки."""
    print(" ", end=" ")


def print_end():
    """
    Печатает пустую строку (перевод на новую строку).
    Завершает текущую строку на экране.
    """
    print()


def nothing():
    """
    Функция-пустышка (аналог "pass").
    Используется как заглушка в шаблоне one_four_one,
    когда какое-то действие не требуется.
    """
    "do nothing"  # Это просто комментарий в теле функции


# ===================================================================
# ФУНКЦИИ СРЕДНЕГО УРОВНЯ (КОМПОНЕНТЫ ЯЧЕЙКИ)
# ===================================================================
def print1beam():
    """
    Печатает одну "балку": "+----".
    Использует шаблон one_four_one с действиями:
    - ничего (nothing)
    - 4 дефиса (print_dash)
    - 1 плюс (print_plus)
    """
    one_four_one(nothing, print_dash, print_plus)


def print1post():
    """
    Печатает один "столб": "|    ".
    Использует шаблон one_four_one с действиями:
    - ничего (nothing)
    - 4 пробела (print_space)
    - 1 вертикальная черта (print_bar)
    """
    one_four_one(nothing, print_space, print_bar)


# ===================================================================
# ФУНКЦИИ ПОСТРОЕНИЯ ЛИНИЙ (ФОРМИРОВАНИЕ РЯДОВ)
# ===================================================================
def print4beams():
    """
    Печатает горизонтальную линию сетки: "+----+----+----+----+"
    Шаблон one_four_one:
    - print_plus (печатает первый +)
    - print1beam (печатает "----+" четыре раза)
    - print_end (перевод строки)
    """
    one_four_one(print_plus, print1beam, print_end)


def print4posts():
    """
    Печатает вертикальные стенки строки: "|    |    |    |    |"
    Шаблон one_four_one:
    - print_bar (печатает первую |)
    - print1post (печатает "    |" четыре раза)
    - print_end (перевод строки)
    """
    one_four_one(print_bar, print1post, print_end)


# ===================================================================
# ФУНКЦИЯ ПОСТРОЕНИЯ РЯДА ЯЧЕЕК
# ===================================================================
def print_row():
    """
    Печатает полный ряд сетки (строку ячеек):
    - 4 раза print4posts (вертикальные стенки + пробелы)
    - 1 раз print4beams (горизонтальная линия с плюсами)
    Шаблон one_four_one:
    - nothing (ничего не делаем сначала)
    - print4posts (вызов 4 раза для создания тела ряда)
    - print4beams (завершающая линия)
    """
    one_four_one(nothing, print4posts, print4beams)


# ===================================================================
# ГЛАВНАЯ ФУНКЦИЯ - ПОСТРОЕНИЕ ВСЕЙ СЕТКИ
# ===================================================================
def print_grid():
    """
    Печатает полную сетку 4x4.
    Шаблон one_four_one:
    - print4beams (верхняя граница сетки)
    - print_row (тело сетки с ячейками - вызывается 4 раза)
    - nothing (ничего не делаем в конце)

    Итоговая структура:
    ┌─────────────────────────────────┐
    │  Верхняя граница (print4beams)  │
    │  ┌───────────────────────────┐  │
    │  │   print_row (ряд 1)       │  │
    │  │   print_row (ряд 2)       │  │
    │  │   print_row (ряд 3)       │  │
    │  │   print_row (ряд 4)       │  │
    │  └───────────────────────────┘  │
    └─────────────────────────────────┘
    """
    one_four_one(print4beams, print_row, nothing)


# ===================================================================
# ЗАПУСК ПРОГРАММЫ
# ===================================================================
print_grid()  # Вызов главной функции - печать сетки 4x4


comment = """
После написания черновика сетки 4×4 я заметил, что многие функции
имеют одинаковую структуру: они делают что-то один раз, затем 
делают что-то другое четыре раза, а потом ещё что-то один раз.

Поэтому я написал функцию one_four_one, которая принимает три 
функции в качестве аргументов: она вызывает первую функцию один раз, 
затем использует do_four для вызова второй функции четыре раза, 
а затем вызывает третью функцию.

После этого я переписал функции print1beam, print1post, print4beams, 
print4posts, print_row и print_grid, используя one_four_one.

Программирование — это исследовательский процесс. Написание черновика 
программы часто даёт вам понимание задачи, что может привести к 
переписыванию кода так, чтобы он отражал структуру решения.
--- Allen
"""

print(comment)


"""
ПРАКТИЧЕСКИЙ ПРИМЕР: 
РАЗРАБОТКА ИНТЕРФЕЙСА
В этой главе разобран подход к процессу разработки функций, которые 
используются вместе.

Строка означает, что bob относится к объекту с типом Turtle, как определено в модуле turtle.
Метод mainloop() ожидает, пока пользователь что-нибудь сделает, хотя 
в данном случае пользователю ничего не остается, кроме как закрыть 
окно.
Создав черепашку, вы можете вызвать метод, чтобы переместить ее в окне. 
Метод похож на функцию, но с несколько иным синтаксисом. Например, 
чтобы переместить черепашку вперед, используется следующий код:
bob.fd(100)
Метод fd() связан с объектом черепашки, который мы назвали bob. Вызов 
метода похож на выполнение запроса: вы просите черепашку по имени bob
двигаться вперед.
Аргумент метода fd() — это расстояние в пикселях, поэтому фактический 
размер пройденного расстояния зависит от разрешения вашего экрана.
Другие методы, которые вы можете вызвать: bk() — для перемещения 
назад, lt() — для поворота влево и rt() — для поворота вправо. 

В качестве аргумента методов lt() и rt() указывается угол в градусах.
Кроме того, каждая черепашка держит перо, которое находится либо 
внизу, либо вверху. 
Если перо опущено, черепашка оставляет след при движении. 
Методы pu() и pd() означают «перо вверх — pen up» и «перо вниз — pen down», соответственно.

Чтобы нарисовать прямой угол, добавьте следующие строки в программу 
(после создания bob и перед вызовом mainloop()):
"""

"""ПРОСТОЕ ПОВТОРЕНИЕ
Скорее всего, вы написали что-то вроде этого:"""


import turtle

# bob = turtle.Turtle()
# print(bob) #<turtle.Turtle object at 0x0000024DBFC75FD0>

# bob.fd(100)
# bob.lt(90)
# bob.fd(100)
# bob.lt(90)
# bob.fd(100)
# bob.lt(90)
# turtle.mainloop()

"""
Мы можем сделать то же самое, но написав меньше кода, если воспользуемся инструкцией for. 
Добавьте показанный ниже код в файл mypolygon.py
и запустите программу снова:
"""

for i in range(4):
    print("Hello!")

"""
Это простейший пример использования инструкции for, позже мы рассмотрим более сложные. 
Но его должно быть достаточно, чтобы вы смогли 
переписать свою программу для рисования квадратов."""
import turtle

bob = turtle.Turtle()
# print(bob) #<turtle.Turtle object at 0x0000024DBFC75FD0>

for i in range(4):
    bob.fd(100)
    bob.lt(90)
# turtle.mainloop()

"""Синтаксис инструкции for похож на синтаксис определения функции. 
У него есть заголовок, который заканчивается двоеточием, и отступ для 
тела. Тело может содержать любое количество инструкций.

Инструкция for также называется циклом (loop), потому что порядок 
выполнения проходит через тело цикла, а затем возвращается к началу. 
В данном случае инструкции в теле цикла выполняются четыре раза.

Этот вариант на самом деле немного отличается от предыдущего кода для 
рисования квадрата, потому что черепашка делает еще один поворот после 
отрисовки последней стороны квадрата. Дополнительный ход занимает 
больше времени, но упрощает код, если мы делаем в цикле одно и то же 
несколько раз. 
Эта версия также приводит черепашку в исходное положение, 
и она смотрит в начальном направлении
"""

"""Задачи
1. Напишите функцию square(), которая принимает параметр t (черепашку). 
Эта функция должна рисовать квадрат с помощью черепашки.
Напишите вызов функции, который передает bob в качестве аргумента функции square(), 
а затем снова запустите программу.

bob = turtle.Turtle() 

for i in range(4):
    bob.fd(100)
    bob.lt(90)
    
Функция square принимает один параметр с именем t
t — это просто имя переменной внутри функции
Внутри функции мы используем t, чтобы обращаться к черепашке

2.Добавьте еще один параметр length в функцию square(). Измените 
тело функции так, чтобы длина сторон была равна length, а затем измените вызов функции, 
чтобы передать ей второй аргумент. Запустите 
программу еще раз. Протестируйте программу, указывая различные 
значения length.
"""


def square(t, length, width):
    print(f"Внутри функции: параметр t = {t}, length = {length}, width = {width}")
    print(f"t и bob - это один объект? {t is bob}")
    for i in range(4):
        t.fd(length)
        t.lt(width)


bob = turtle.Turtle()
square(bob, 100, 90)  # Передаём значение переменной bob, длину и ширину стороны
print(f"После функции: bob переместился!")
# turtle.done()


"""
3.Скопируйте код функции square() и измените ее имя на polygon(). 
Добавьте параметр n и измените тело функции так, чтобы она рисовала 
n-сторонний правильный многоугольник.
Подсказка: внешние углы n-стороннего правильного многоугольника равны 360/n градусов.
"""


def polygon(t, length, n):
    """
    Рисует правильный n-сторонний многоугольник с помощью черепашки t.

    Аргументы:
        t: объект черепашки
        length: длина стороны
        n: количество сторон
    """
    angle = 360 / n  # Внешний угол для правильного многоугольника

    for i in range(n):
        t.fd(length)
        t.lt(angle)  # Поворачиваем на внешний угол


# Создаем черепашку
bob = turtle.Turtle()
bob.speed(5)

# Тестируем с разными многоугольниками
polygon(bob, 85, 3)  # Треугольник (сторона 50, 3 стороны)
bob.penup()
bob.goto(100, 0)
bob.pendown()
polygon(bob, 50, 5)  # Пятиугольник
bob.penup()
bob.goto(200, 0)
bob.pendown()
polygon(bob, 30, 8)  # Восьмиугольник
bob.penup()
bob.goto(300, 0)
bob.pendown()
polygon(bob, 20, 12)  # Двенадцатиугольник (почти круг)

# turtle.done()


"""Инкапсуляция
В первом упражнении требовалось поместить код для рисования квадрата 
в определение функции, а затем вызвать эту функцию, передав черепашку 
в качестве параметра. Ниже представлено решение.
def square(t):
for i in range(4):
    t.fd(100)
    t.lt(90)
    
square(bob) 

Внутренние инструкции fd и lt имеют двойной отступ, показывая, что 
они находятся внутри цикла for, а тот, в свою очередь, находится внутри 
определения функции. Следующая строка, square(bob), не имеет отступов 
слева, что указывает на конец как цикла for, так и определения функции.
Внутри функции t относится к той же черепашке bob, поэтому t.lt(90)
имеет тот же результат, что и bob.lt(90). В таком случае, почему бы не вызвать 
параметр bob? 

Суть в том, что t может быть любой черепашкой, а не только 
bob, поэтому вы можете создать вторую черепашку и передать ее в качестве 
аргумента функции square():
alice = Turtle()
square(alice)

Заключение фрагмента кода в функцию называется инкапсуляцией 
(encapsulation). 
Одно из преимуществ инкапсуляции в том, что она позволяет 
присвоить фрагменту кода имя, которое помогает документировать код. 
Другое преимущество состоит в том, что, если вы повторно используете код, 
то более корректным будет - вызвать функцию два раза, а не копировать/вставлять тело!
"""

"""
ОБОБЩЕНИЕ
На следующем шаге в функцию square() добавляется параметр length. Ниже 
показано решение:
def square(t, length):
    for i in range(4):
        t.fd(length)
        t.lt(90)

square(bob, 100)

Добавление параметра в функцию называется обобщением, или — 
иногда — универсализацией, поскольку в результате функция становится 
более универсальной: в предыдущей версии квадрат всегда был одинакового 
размера. 
А в этой версии размер может быть любой.

Следующий шаг также является обобщением. Вместо того чтобы рисовать 
квадраты, функция polygon() рисует обычные многоугольники с любым 
числом сторон. Ниже показано решение:

def polygon(t, n, length):
    angle = 360 / n
    for i in range(n):
        t.fd(length)
        t.lt(angle)
        
polygon(bob, 7, 70)


В этом примере рисуется семисторонний многоугольник с длиной стороны 70.

Если функция имеет несколько числовых аргументов, легко забыть, что 
они собой представляют или в каком порядке должны перечисляться. В этом 
случае рекомендуется в список аргументов включать имена параметров:

polygon(bob, n=7, length=70)

Это так называемые ключевые аргументы (keyword arguments), потому 
что они включают в себя имена параметров в качестве «ключевых слов» 
(не путать с зарезервированными ключевыми словами Python, такими как 
while и def).
Такой синтаксис упрощает понимание кода программы. Это также напоминание о том, 
как работают аргументы и параметры: когда вы вызываете 
функцию, значения аргументов присваиваются параметрам.
"""

"""
РАЗРАБОТКА ИНТЕРФЕЙСА
На следующем шаге нужно написать функцию circle(), которая принимает 
радиус r в качестве параметра. Ниже показано простое решение, 
где используется функция polygon() для рисования многоугольника с 50 сторонами:
"""
import math


def circle(t, r):
    circumference = 2 * math.pi * r
    n = 50
    length = circumference / n
    polygon(t, n, length)


"""
Первая строка вычисляет длину окружности круга с радиусом r по формуле 2πr. 
Так как мы используем свойство math.pi, мы должны импортировать модуль math. 
По соглашению инструкции импорта обычно указываются 
в начале скрипта.

Число n — это количество отрезков в нашей окружности, а length — длина 
каждого отрезка. Таким образом, функция polygon() рисует 50-сторонний 
многоугольник, который приближен по форме к кругу с радиусом r.

Одно из ограничений этого решения заключается в том, что n — это 
константа, а значит, для очень больших кругов отрезки линии слишком 
длинные, а для маленьких кругов мы тратим время на рисование очень маленьких отрезков. 

Одно из решений — обобщить функцию, задав n в качестве 
параметра. Это дало бы пользователю (кто бы ни вызывал функцию circle) 
больше контроля, но интерфейс был бы менее понятен.


Интерфейс (interface) функции представляет собой краткое изложение 
того, как ее использовать: какие параметры? Что делает функция? Что 
представляет собой возвращаемое значение?

Интерфейс «понятен», если он позволяет программисту вызывать функцию так, как он хочет, 
не касаясь ненужных деталей.
В этом примере r принадлежит интерфейсу, потому что определяет 
круг, который следует нарисовать. 
Значение n менее важно, потому что оно относится к деталям того, как круг должен быть отображен.
Вместо того чтобы загромождать интерфейс, лучше выбрать подходящее значение n в зависимости 
от размера окружности:
"""


def circle(t, r):
    circumference = 2 * math.pi * r
    n = int(circumference / 3) + 1
    length = circumference / n
    polygon(t, n, length)


"""Теперь число сегментов — это целое число, приблизительно равное 
circumference/3, поэтому длина каждого сегмента составляет приблизительно 3, 
что достаточно мало, чтобы круги выглядели хорошо, но достаточно велико, 
чтобы функция была эффективна и приемлема для круга любого размера."""


"""
РЕФАКТОРИНГ
Когда я писал функцию circle(), я мог повторно использовать функцию 
polygon(), потому что многогранный многоугольник приблизительно похож 
на круг. Но функция arc() не так дружелюбна; мы не можем использовать 
функцию polygon() или circle(), чтобы нарисовать дугу.
Один из вариантов — начать с дублирования функции polygon() и преобразовать ее в arc(). 
Результат будет выглядеть так:
"""

def arc(t, r, angle):
    arc_length = 2 * math.pi * r * angle / 360
    n = int(arc_length / 3) + 1
    step_length = arc_length / n
    step_angle = angle / n
    for i in range(n):
        t.fd(step_length)
        t.lt(step_angle)

"""
Вторая часть функции выше выглядит как функция polygon(), но мы 
не можем повторно использовать функцию polygon() без изменения интерфейса. 
Мы можем обобщить, то есть универсализировать, функцию polygon(), 
передав угол в качестве третьего аргумента, но тогда имя polygon() больше 
не годится! Вместо этого давайте назовем ее общим именем polyline():
"""

def polyline(t, n, length, angle):
    for i in range(n):
        t.fd(length)
        t.lt(angle)

"""Теперь мы можем переписать функции polygon() и arc(), чтобы они 
использовали функцию polyline():"""

def polygon(t, n, length):
    angle = 360.0 / n
    polyline(t, n, length, angle)
    

def arc(t, r, angle):
    arc_length = 2 * math.pi * r * angle / 360
    n = int(arc_length / 3) + 1
    step_length = arc_length / n
    step_angle = float(angle) / n
    polyline(t, n, step_length, step_angle)

"""Наконец, мы можем переписать функцию circle(), чтобы она использовала функцию arc():"""

def circle(t, r):
    arc(t, r, 360)
    
"""Этот процесс — изменения структуры программы для улучшения интерфейсов и 
упрощения повторного использования кода — называется 
рефакторингом (refactoring). В данном случае мы заметили, что в функциях 
arc() и polygon() используется похожий код, поэтому мы «вынесли его» 
в функцию polyline().
Если бы мы планировали заранее, мы могли бы сначала написать функцию polyline() 
и избежать рефакторинга, но в начале проекта недостаточно 
информации для проработки всех интерфейсов. 
А уже в процессе разработки вы начинаете лучше понимать проблему. 
Иногда рефакторинг — признак того, что вы чему-то научились."""


"""
СПОСОБ РАЗРАБОТКИ
Способ разработки (development plan) — это подход или метод в процессе 
написания программ. 
Метод, который мы использовали в этом примере: 
«инкапсуляция и обобщение». Шаги такие.

1. Начните с написания небольшой программы без определения функций.

2. Как только вы запустите программу и она будет работать, найдите связанную 
часть кода и инкапсулируйте ее в функцию, присвойте ей имя.

3. Обобщите функцию, добавив подходящие параметры.

4. Повторяйте шаги 1–3, пока не получите набор рабочих функций. 
Копируйте и вставляйте работающий код, чтобы избежать повторного 
ввода (и повторной отладки).

5. Ищите возможности улучшить программу путем рефакторинга. 
Например, если у вас есть похожий код в нескольких местах, рассмотрите возможность
его преобразования в универсальную функцию!!!

Стро 68
"""
